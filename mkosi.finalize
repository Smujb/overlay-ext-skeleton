#!/usr/bin/env bash

set -oex

# We want to make sure that the sysext is locked to the current IMAGE_VERSION and ID
cat > ${BUILDROOT}/usr/lib/extension-release.d/extension-release.${OUTPUT_NAME} <<EOF
ID=${BASE_IMAGE_ID}
VERSION_ID=${IMAGE_VERSION}
EOF
